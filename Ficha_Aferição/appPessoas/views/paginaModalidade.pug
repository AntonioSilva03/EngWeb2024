extends layout 

block content 
    .w3-card-4
        header.w3-container.w3-blue
            h1 #{titulo}
        .w3-container
            table.w3-table-all
                tr
                    th Nome 
                each item in modalidade
                    tr
                        td
                            a(href="/pessoas/" + item._id)= item.nome
        footer.w3-container.w3-blue
            h5 
                span [ 
                    a(href="/modalidades") Voltar à lista de modalidades 
                span ]